{
  "name": "@flow-designer/parity",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "M5 parity harness — renders the deck's real flow definitions through the NEW shared library (@flow-designer/library) so the swap can be visually validated against the current deck/components/flow/* rendering. Scratch/validation tooling, NOT a shipped artefact.",
  "scripts": {
    "dev": "vite"
  },
  "dependencies": {
    "@flow-designer/library": "workspace:*",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^6.0.0"
  }
}
