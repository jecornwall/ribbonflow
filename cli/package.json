{
  "name": "@ribbonflow/cli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Flow-collection build CLI — validates/normalizes/migrates a directory of *.flow.json through @ribbonflow/core, then emits a referenceable asset bundle and/or a hostable static gallery. Becomes @ribbonflow/cli at the repo split.",
  "bin": {
    "ribbonflow": "./bin/ribbonflow.js"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@ribbonflow/core": "workspace:*",
    "ribbonflow": "workspace:*",
    "esbuild": "^0.25.0"
  }
}
